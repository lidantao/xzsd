<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xzsd.app.home.dao.HomeDao">
<!--    查询首页轮播图接口-->
    <select id="listRotate" parameterType="java.lang.String" resultType="com.xzsd.app.home.entity.HomeInfo">
        select
        goods_code          as      goodsCode,
        rotate_image_path   as      goodsImagePath
        from t_sys_rotate
        where   is_delete           =      0

        order by    create_time
    </select>
<!--    分页查询热门商品接口-->
    <select id="listHotGoodsByPage" resultType="com.xzsd.app.home.entity.HomeInfo">
        select
        a.goods_code        as      goodsCode,
        b.goods_name        as      goodsName,
        b.goods_image_path  as      goodsImagePath,
        b.goods_price       as      goodsPrice
        from    t_sys_hot_goods a,  t_sys_goods b
        where   a.is_delete     =   0
        and     a.goods_code    =   b.goods_code
        order by create_time
    </select>
</mapper>